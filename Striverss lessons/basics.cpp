#include <iostream>
using namespace std;

int main() {
    // strings
    // string str;
    // getline(cin, str); // Read a line of input into str
    // cout << "String: " << str;
    // return 0;

    // loops
    int age;
    cin >> age; // Read an integer input into age
    if (age < 18) {
        cout << "You are a minor." << endl;
    } else {
        cout << "You are an adult." << endl;
    }
}   
